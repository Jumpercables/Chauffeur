<?xml version="1.0" encoding="UTF-8"?>
<Wix xmlns="http://schemas.microsoft.com/wix/2006/wi">

  <!--
   Unattended setup: The following variables can be set:
   -->

  <!--
      ====================================================================================
      Defines & Variables
   -->
  <?ifndef $(var.BuildArtifactDir)?>  
  <?define BuildArtifactDir = "..\Chauffeur\bin\x86\Debug"?>
  <?endif?>

  <!--
      ====================================================================================
      Package start
   -->
	<Product Id="*" Name="Chauffeur Windows Service" Language="1033" Version="1.0.0.0" Manufacturer="Metis LLC" UpgradeCode="bc63b526-be6d-411e-9bd7-ba707c91bbd3">

    <!-- Package IDs are valid for a single package version only - they are autogenerated by WiX -->
    <!-- Let's require Windows Installer 4.0 (included in Vista) -->
    <Package InstallerVersion="200"
             Compressed="yes"
             InstallScope="perMachine"
             Description="Chauffeur (Jenkins) Service for Automated Installations of Builds."/>

    <!-- Set properties for add/remove programs -->
    <Property Id="PRODUCTNAME" Value="Chauffeur"/>
    <Property Id="PRODUCTVERSION" Value="$(var.ProductVersion)"/>
    <Property Id="ARPNOREPAIR" Value="yes" Secure="yes" />
    <Property Id="ARPNOMODIFY" Value="yes" Secure="yes" />
   
		<MajorUpgrade DowngradeErrorMessage="A newer version of [ProductName] is already installed." />
		<MediaTemplate />

		<Feature Id="ProductFeature" Title="Window Service" Level="1">
			<ComponentGroupRef Id="ProductComponents" />
		</Feature>
	</Product>

	<Fragment>
		<Directory Id="TARGETDIR" Name="SourceDir">
			<Directory Id="ProgramFilesFolder">
				<Directory Id="INSTALLFOLDER" Name="Chauffeur" />
			</Directory>
		</Directory>
	</Fragment>

	<Fragment>
		<ComponentGroup Id="ProductComponents" Directory="INSTALLFOLDER">
      <Component Id="ProductComponent">
        <File Id="Chauffeur.exe" Source="$(var.BuildArtifactDir)\Chauffeur.exe" />
        <File Id="Chauffeur.exe.config"  Source="$(var.BuildArtifactDir)\Chauffeur.exe.config" />
      </Component>
		</ComponentGroup>
	</Fragment>
</Wix>